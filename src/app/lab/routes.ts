import { Routes } from '@angular/router';
import { LabComponent } from './lab.component';

export default [
  {
    path: 'lab', // Define the route path
    component: LabComponent // Associate it with the component
  },
] as Routes;
